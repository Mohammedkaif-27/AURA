# AURA Backend Environment Variables Template
# Copy this to .env and fill in your actual values

# ===== REQUIRED FOR CLOUD RUN DEPLOYMENT =====

# Google Cloud Project Configuration
PROJECT_ID=your-project-id-here
REGION=asia-south1

# ===== SMTP EMAIL CONFIGURATION (for notifications) =====

# SMTP Server Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Gmail Credentials (use App Password, not regular password)
# Generate App Password: https://myaccount.google.com/apppasswords
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password-here
SENDER_EMAIL=your-email@gmail.com

# ===== OPTIONAL (for local development only) =====

# Google Application Credentials (not needed for Cloud Run)
# GOOGLE_APPLICATION_CREDENTIALS=backend/service_account.json

# Local server port (Cloud Run will override this with PORT=8080)
# PORT=8080

# ===== DEPLOYMENT NOTES =====

# 1. For Cloud Run deployment, you DON'T need .env file
#    Set environment variables directly using gcloud command:
#
#    gcloud run deploy aura-backend \
#      --source . \
#      --region asia-south1 \
#      --set-env-vars PROJECT_ID=your-project,REGION=asia-south1,...

# 2. For local Docker testing:
#    docker run -p 8080:8080 --env-file .env aura-backend

# 3. For local development (uvicorn):
#    Create .env file with the values above
#    Run: uvicorn backend.main:app --reload
