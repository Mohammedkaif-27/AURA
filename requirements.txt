# FastAPI and web server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0


# LLM providers
groq>=0.4.0
openai>=1.0.0

# Embeddings — fully local via sentence-transformers
sentence-transformers>=2.2.0
transformers>=4.30.0

# Numpy (lightweight, used for mean-pooling API embeddings)
numpy>=1.24.0

# File uploads for FastAPI
python-multipart>=0.0.6

# Vector database
chromadb==1.5.9

# PDF processing (PyMuPDF — better reading order & heading preservation)
PyMuPDF>=1.23.0

# PDF extraction fallback (pdfplumber — used in rag.py _extract_pages_pdf)
pdfplumber>=0.10.0
pypdf>=3.0.0

# OCR for scanned PDFs (pytesseract — used in rag.py)
Pillow>=10.0.0
pytesseract>=0.3.10

# DOCX processing
python-docx>=0.8.11

# Hybrid search (BM25 keyword index)
rank-bm25>=0.2.2

# Environment and configuration
python-dotenv>=1.0.0

# Supabase (optional — falls back to local JSON)
supabase>=2.0.0

# Email
# (uses stdlib smtplib — no extra deps)
resend>=0.7.0

# Data imports
pandas>=2.0.0
openpyxl>=3.1.0
python-pptx>=0.6.21
